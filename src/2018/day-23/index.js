import {
	total_number,
	shortest_distance,
} from "./lib.js";

/**
 * @param {string} content
 */
export function main(content) {
	console.log(total_number(content));
	console.log(shortest_distance(content));
}

import {
	grand_total,
	grand_total2,
} from "./lib.js";

/**
 * @param {string} content
 */
export function main(content) {
	console.log(grand_total(content));
	console.log(grand_total2(content));
}

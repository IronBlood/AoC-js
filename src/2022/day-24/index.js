import {
	fewest_minutes,
	fewest_minutes2,
} from "./lib.js";

/**
 * @param {string} content
 */
export function main(content) {
	console.log(fewest_minutes(content));
	console.log(fewest_minutes2(content));
}

import {
	sum_quality_levels,
	multiply_geodes,
} from "./lib.js";

/**
 * @param {string} content
 */
export function main(content) {
	console.log(sum_quality_levels(content));
	console.log(multiply_geodes(content));
}
